let score = "100";

console.log("Data type is " + typeof(score))

//lets convert the data type from string to number
let numScore = Number(score);

console.log("New Data type is " + typeof(numScore));


// So to convert use the syntax
//  let x = datatype(variable);

// if it was null the number value will be 0
// it string is 22abc then the number is NaN
// true -> 1
// false -> 0

// in string to boolean
// "" -> false
// "h" -> true

// to string conversion
// number to string

//operations
// use ** for power

// Whenever you need to compare 2 values
// make sure the 2 values are of same data-type

// === strict check, also check datatypes